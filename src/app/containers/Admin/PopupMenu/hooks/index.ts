import { usePopupMenus } from './usePopupMenus';
import { usePopupMenu } from './usePopupMenu';
import { useCreatePopupMenu } from './useCreatePopupMenu';
import { useUpdatePopupMenu } from './useUpdatePopupMenu';
import { useDeletePopupMenu } from './useDeletePopupMenu';

export const popupMenusHooks = {
  usePopupMenus,
  usePopupMenu,
  useCreatePopupMenu,
  useUpdatePopupMenu,
  useDeletePopupMenu,
};
